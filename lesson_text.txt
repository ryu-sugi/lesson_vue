  ※Vueのオプション構成]
  ※書式→ var app = new Vue({プロパティ名: 値, プロパティ名: 値, プロパティ名: 値, ...})

var app = new Vue ({
  el: '#app',
  data: {
     「コンポーネントが保持するデータをここに定義する」
  },
  methods: {
      「コンポーネントが保持するメソッドをここに定義する」
  },
  filters: {
      「コンポーネントが保持するフィルターをここに定義する」
  },
  computed: {
      「コンポーネントが保持するプロパティをここに定義する」
  },
  watch: {
      「コンポーネントが保持するウォッチャをここに定義する」
  }
});

